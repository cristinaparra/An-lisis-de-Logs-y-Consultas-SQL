# An-lisis-de-Logs-y-Consultas-SQL | Bootcamp TripleTen (2025)
# Sprint 7 — Análisis de Logs y SQL

## 📝 Descripción
Entrega correspondiente al Sprint 7 del Bootcamp de QA Engineer, enfocada en el análisis de logs mediante consola y en la realización de consultas SQL para validar, filtrar y estructurar datos.  
El trabajo incluyó filtrado de IPs, extracción y clasificación de eventos HTTP, y consultas avanzadas a bases de datos utilizando funciones agregadas, rangos de tiempo y lógica condicional.  
El proyecto fue evaluado como altamente satisfactorio, con comandos precisos, organización clara y correcto uso de herramientas de análisis.

---

## ⭐ Habilidades Destacadas
- Uso de expresiones regulares en consola para filtrado de datos.
- Manejo eficiente de `grep` para búsqueda recursiva en logs.
- Organización de archivos y creación de estructuras de carpetas para clasificación de datos.
- Extracción y separación de eventos 400 y 500 provenientes de registros reales.
- Construcción de consultas SQL con:
  - `COUNT`, `DISTINCT`, `GROUP BY`, `HAVING`, `ORDER BY`
  - Filtrado por rangos de fecha
  - Clasificación de datos mediante `CASE`
  - Uso de `ILIKE` para coincidencias no sensibles a mayúsculas
  - JOINs entre tablas para análisis de relaciones
- Validación, depuración y análisis de datos estructurados y no estructurados.
- Implementación de buenas prácticas como rangos medio-abiertos para timestamps.

---

## 🧰 Herramientas Utilizadas
- **Linux / Bash**
- **grep** con expresiones regulares
- **PostgreSQL**
- Comandos de consola:
  - `grep -R`
  - redirección `>` para crear archivos
  - creación de carpetas (`mkdir`)
- SQL:
  - funciones agregadas (`COUNT`, `DISTINCT`)
  - `GROUP BY`, `HAVING`, `ORDER BY`
  - `JOIN`
  - `ILIKE`
  - `CASE`
  - manejo de `timestamp` y rangos de tiempo

